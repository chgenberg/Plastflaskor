# 2026-09-03

- Analys av Billboardbee-2.0 (fem utforskare: köpflöde, dashboard, designsystem, agent-metod, orderkedja). BBB kortar landning→order till 5–6 skärmar via detaljsida-som-kassa, konto i köpögonblicket, serverpris, material efteråt. Multi-turn-agent och cart-UI är avvecklade i BBB:s kod.
- Produktbeslut: ny kund registrerar sig i kassa-modalen, får STANDARD automatiskt, ser pris först efter registrering, lägger order direkt. Mål ≤ 4 skärmar. Inga kr före konto.
- Masterplan skriven: `docs/masterplan/00-INDEX.md` + 01–10 (tio skrivare parallellt, alla radnummer verifierade i båda repona). Index §3 låser 16 bindande beslut där filerna sa olika: tre actions i `src/actions/checkout.ts`, scheman i `src/domain/schemas/checkout.ts`, steg A = konto / steg B = pris+adress+referens, Company→Customer→User i transaktion, `Order.clientToken`, `Customer.source/verifiedAt`, redirect direkt till `/kassa/bekraftelse`, node:test (ingen vitest), `.av-lift` → `.av-card-lift`.
- Fynd att åtgärda i Våg D1: `tokens.css` 231–253 har `position: relative` på `<tr>` + `.av-row-hit::after inset:0` — Safari-buggen BBB varnar för. Flytta relative till första `<td>`.
- Fynd: accentblå på `--av-stage #0b0d10` ger 2.9:1 — `--av-stage-accent` = blue-100 för text på mörk yta.
- Workboard: `wb-bbb-masterplan` done. Elva `wb-mp-*`-kort inlagda (A/B/C/D1/D2 + push-kort med gate deploy, E ready). Ingen kod rörd.
- Våg E dag 1 + Våg A implementerade lokalt. Prisma: `Customer.source/verifiedAt`, `Order.receivedMailSentAt/clientToken`. Kassan: `registerCheckoutAction` + `placeCheckoutOrderAction`, modal A/B, `/kassa` + `/kassa/bekraftelse`. `npm test` 33/33. `qa:no-kr` grön mot localhost. Service-rök: self_signup + STANDARD + AQUA_REVIEW utan snapshot. Push-kort `wb-mp-a-push` väntar på walkthrough + bug-hunt + explicit ja.
- Skylt-lager låst som regel: `.cursor/rules/folder-boundaries.mdc` + README i `src/{app,actions,server,ui,domain,lib}` och `audits/`, `docs/`. Ingen filflytt. ESLint förbjuder Next/React i domain och Prisma i UI.
- Prisma-luckor i `src/app/` stängda: kassa, konto-layout, ny order, kunder, priser, produktion, documents-API. Läsningar bor i services. ESLint förbjuder `@/server/db` i app.
- Våg B implementerad lokalt. Produktsida är kassa: chips, MOQ, artwork-val, sticky `#bestall`, mobilbar utan pris, Beställ → OrderModal. Icke-WATER = Begär offert. Kategorikort = en länk, "Konfigurera →". Startsida tre akter + stödsektioner. Start hämtar `listHomeProducts` (namn/slug/moq/leadTime, ingen specText i HTML). `npm test` 35/35. `qa:no-kr` grön. PublicNav over-hero hoppades över (valfritt). Push-kort `wb-mp-b-push` väntar på walkthrough + bug-hunt + explicit ja.
- Bug-hunt våg B (read-only): 0 CRITICAL, 7 HIGH (Stenkulla-lime, capLabel, saknat main, 10 px scenprickar, StepScrollScrub-CLS, Reveal dold utan JS, villkor hidden). Inte lagade i C utom Reveal-gaten och `<main>` på start.
- Våg C implementerad lokalt. `.av-lift` → `.av-card-lift`. Reveal gatad på `html.js`. Header på `/` är `av-site-header` + `data-scrolled`. `shape="pill"` på PillLink. `FeatureChip`, publik `Faq`, Parallax i `(public)/layout`. `npm test` 35/35. `qa:no-kr` grön. Push-kort `wb-mp-c-push` väntar på bug-hunt + explicit ja.
- Våg B HIGH lagade (0 CRITICAL fanns). `parseWaterKind` skiljer citron/lime från kolsyrat; konfiguratorn visar tre chips och matchar `stenkulla-33-lime`. Order-schema får fortfarande `waterType: kolsyrat` (variantId är lime-SKU). `capLabel("skruvkork")` = SKRUVKORK. Hero-scenprickar 24×24. `.av-step-scrub` reserverar `N*100svh` på desktop+js. Synlig `acceptTerms`-kryss. `npm test` 38/38. `qa:no-kr` grön. Workboard `wb-mp-b-high-fixes` done.
- Våg D1+D2 implementerade lokalt. `NAV_BY_ROLE` i `src/ui/shell/nav.ts`. AppShell: fixed aside, topbar/tabbar/drawer, fot med Inställningar bara för Aqua. `statusHint` + `customerActionFor`. `/konto` hem: NeedsAttention → KPI → 5 ordrar → QuickLinks. Peek: tidslinje överst, en primär åtgärd, `?steg=artwork|korr`. Safari-rad: relative på första `td`. Ops-hem åtgärds-KPI + kundverifiering + `ArtworkReviewCard`/`rejectArtwork` (`AQUA_REJECTED`, status orörd). SupplierDesk KPI utan kr. `npm test` 45/45. `qa:no-kr` grön. Push-kort `wb-mp-d1-push` / `wb-mp-d2-push` väntar på walkthrough + bug-hunt + explicit ja.
- Pre-push hunt A–D mot working tree: 0 CRITICAL, 1 HIGH. Reject artwork anropar `sendArtworkApproval` → notis "Korrektur att godkänna"; `customerActionFor` ignorerar `AQUA_REJECTED`. Rapport `audits/prepush/2026-09-03-wb-mp-ad-push.md`. Push stoppad trots explicit ja. MEDIUM: sticky-vatten, leverantör-NeedsAttention, AppShell-fallback path, 32 px listknapp.
- HIGH lagad: `sendArtworkRejected` + `needsNewArtworkAfterReject`. Omjakt 0 CRITICAL / 0 HIGH. `qa:no-kr` 12 sidor OK. Push-kort A–D2 done.
