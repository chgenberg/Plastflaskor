# `src/lib/` — isomorft

Ren hjälp som får köras utan Prisma och utan secrets.
`orchestrator/` (karta, workboard, grindar, Hands) bor här.

Databas, session och `cookies()` hör hemma i `src/server/`.
Domänregler hör hemma i `src/domain/`.
